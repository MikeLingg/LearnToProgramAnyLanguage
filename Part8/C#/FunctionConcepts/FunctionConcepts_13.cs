// Examples of using a Main method and accessing program arguments
// Main method with no parameters - arguments are ignored
using System;

class Program
{
    static void Main ()
    {
        Console.WriteLine ( "Main method with no parameters - command line arguments ignored" );
    }
}
